---
kind: pipeline
name: default

steps:
- name: lint
  image: centos
  commands:
    - yum update && yum install -y make ShellCheck
    - make
    - shellcheck ~/.bashrc
    - shellcheck ~/.bash_profile