---
kind: pipeline
name: Pipeline

steps:
- name: lint
  image: archlinux:latest
  commands:
    - pacman -Sy --noconfirm git make shellcheck vim
    - make
    - shellcheck ~/.bashrc
    - shellcheck ~/.bash_profile